#!monit
set logfile /var/log/monit.log

check process home-node with pidfile "/var/run/home-node.pid"
    start program = "/sbin/start home-node"
    stop program  = "/sbin/stop home-node"
    if failed port 8096 protocol HTTP
        request /health
        with timeout 10 seconds
        then restart
